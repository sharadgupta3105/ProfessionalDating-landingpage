# Copy to Netlify → Site configuration → Environment variables

LINKEDUP_API_URL=https://api.matchedin.app
LINKEDUP_SUPPORT_EMAIL=help@matchedin.app
# Proxy API via your Netlify domain (recommended; avoids HTTPS→HTTP mixed content)
LINKEDUP_API_PROXY=true
